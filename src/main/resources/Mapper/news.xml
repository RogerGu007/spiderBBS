<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.school.dao.INewsDAO">
    <sql id="ColumnList">
      id,
      title mSubject,
      news_type mNewsType,
      news_job_sub_type mNewsJobSubType,
      is_hot mIsHot,
      our_article_url mOurArticleUrl,
      comment_count mCommentCount,
      gallary_image_count mGallaryImageCount,
      post_date mPostDate,
      update_at updateAt,
      update_by updateBy,
      create_at createAt,
      create_by createBy
    </sql>

    <sql id="FullColumn">
        id,
        <include refid="ColumnList"/>
    </sql>

    <select id="selectAllNews" resultType="com.school.entity.News">
        select <include refid="FullColumn"/> from news
    </select>

    <insert id="addRecord" parameterType="com.school.entity.News">
        insert into news
        (title, news_type, news_job_sub_type, is_hot, our_article_url, comment_count, gallary_image_count)
        values
        (#{mSubject}, #{mNewsType}, #{mNewsJobSubType}, 0, #{mOurArticleUrl}, 0, 0)
    </insert>

</mapper>